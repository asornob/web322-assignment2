<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= project.name %> - Climate Solutions</title>
  <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-base-200 text-base-content">
  <header class="p-6 shadow-md bg-base-100">
    <h1 class="text-3xl font-bold text-center">Climate Solutions</h1>
    <h2 class="text-xl text-center mt-2"><%= project.name %></h2>
  </header>

  <main class="container mx-auto p-10 text-center">
    <% if (project) { %>
      <div class="bg-white shadow-lg rounded-lg p-8 inline-block text-left">
        <h3 class="text-2xl font-bold mb-2"><%= project.name %></h3>
        <p class="text-gray-700 mb-2"><strong>Sector:</strong> <%= project.sector %></p>
        <p class="text-gray-700 mb-2"><strong>Location:</strong> <%= project.location %></p>
        <p class="text-gray-600 mb-4"><%= project.description %></p>
        <a href="/solutions/projects" class="text-blue-600 hover:underline font-semibold">← Back to Projects</a>
      </div>
    <% } else { %>
      <p class="text-gray-600 text-lg mt-10">Project not found.</p>
      <a href="/solutions/projects" class="text-blue-600 hover:underline">Go Back</a>
    <% } %>
  </main>

  <footer class="text-center p-6 text-gray-500">
    <p>Assignment 2 - Al Sad Ornob (130207236)</p>
  </footer>
</body>
</html>
