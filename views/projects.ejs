<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Projects - Climate Solutions</title>
  <link href="/css/main.css" rel="stylesheet" />
</head>

<body class="bg-base-200 text-base-content">
  <header class="p-6 shadow-md bg-base-100">
    <h1 class="text-3xl font-bold text-center">Climate Solutions</h1>
    <h2 class="text-xl text-center mt-2"><%= title || "All Projects" %></h2>
  </header>

  <main class="container mx-auto p-10">
    <% if (projects && projects.length > 0) { %>
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% projects.forEach((p) => { %>
          <div class="p-6 bg-white rounded-lg shadow hover:shadow-xl transition">
            <h3 class="text-lg font-semibold mb-2">
              Sector: <%= p.sector %>
            </h3>
            <p class="text-gray-600 mb-4"><%= p.name %></p>
            <a href="/solutions/projects/<%= p.id %>" 
               class="text-primary font-semibold hover:underline">
               View Details
            </a>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-center text-lg mt-10 text-gray-600">
        No projects available.
      </p>
    <% } %>
  </main>

  <footer class="text-center p-6 text-gray-500">
    <p>Assignment 2 - Al Sad Ornob (130207236)</p>
  </footer>
</body>
</html>
